import React from "react"
import Layout from "../components/layout"
import Pannel from "../components/pannel"

export default () => {
    return (
        <Layout>
            <Pannel />
        </Layout>
    )
}